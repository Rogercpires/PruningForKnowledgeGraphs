# Top destinos de um IP
SELECT ?dst (COUNT(*) AS ?n)
WHERE {
  GRAPH <http://ex.org/graph_temporal_24h> {
    ?f <ex:hasSourceAddress> <ip/192.168.10.5> ;
       <ex:hasDestinationAddress> ?dst .
  }
}
GROUP BY ?dst ORDER BY DESC(?n)
